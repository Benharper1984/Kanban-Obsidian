{
	"name": "kanban-4000",
	"version": "0.1.0",
	"description": "A zoomable Trello-like interface for Obsidian",
	"main": "main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
		"version": "node version-bump.mjs && git add manifest.json versions.json"
	},
	"keywords": [
		"obsidian",
		"kanban",
		"plugin"
	],
	"author": "Ben Harper",
	"license": "MIT",
	"devDependencies": {
		"@types/node": "^20.10.0",
		"builtin-modules": "^3.3.0",
		"esbuild": "^0.19.8",
		"obsidian": "latest",
		"typescript": "5.3.2"
	}
}
